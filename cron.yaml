cron:
- description: "Daily Orders Count Update job in Products"
  url: /tasks/summary
  target: products
  schedule: every 10 mins
  retry_parameters:
    min_backoff_seconds: 2.5
    max_doublings: 5