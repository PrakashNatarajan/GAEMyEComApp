cron:
- description: "Daily Orders Count Update job in Products"
  url: /admin/products/orderscount
  target: products
  schedule: every 50 mins
  retry_parameters:
    min_backoff_seconds: 2.5
    max_doublings: 5